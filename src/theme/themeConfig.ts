// src/theme/themeConfig.ts
import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
    token: {
        fontSize: 14, // Tamanho da fonte padrão
        colorPrimary: '#007bff', // Cor primária principal
        colorLink: '#1890ff', // Cor dos links
        borderRadius: 4, // Raio padrão das bordas
        colorSuccess: '#52c41a', // Cor de sucesso
        colorError: '#ff4d4f', // Cor de erros
        colorWarning: '#faad14', // Cor de aviso
        controlHeight: 40, // Altura dos botões, campos, etc.

    },
};

export default theme;
